# Django backend (do NOT ignore the whole backend/ tree ΓÇö source must be tracked)
backend/venv/
backend/.env
backend/.env.*
backend/env/.env
backend/*.sqlite3
db.sqlite3
**/db.sqlite3
backend/**/__pycache__/
backend/**/*.py[cod]
backend/staticfiles/
backend/uploads/
backend/logs/
backend/*.log

# Next.js / frontend
frontend/node_modules/
frontend/.next/
frontend/.env.local
frontend/.env*.local

# Repo root venv (if used)
venv/
.venv/
.venv-local/
ENV/

# IDE / OS
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# Misc
*.pyc
.pytest_cache/
*.rar
FSERP.rar

# Windows: reserved name `nul` cannot be versioned; breaks `git add` (see e.g. accidental `> nul` in wrong cwd)
nul
**/nul

.git.broken/
.git.bak/

