# Copy to `.env`. For local Django, use `frontend/.env.development` (committed) or
# `.env.development.local` (gitignored). Never put `NEXT_PUBLIC_*` localhost URLs in `.env.local`
# — that file applies to `next build` and would ship a broken VPS bundle.
#
# `next build` also loads `.env.production` (committed) with public API URLs — no loopback in prod bundles.

PORT=3000
NEXT_PUBLIC_API_URL=https://api.mahasoftcorporation.com
NEXT_PUBLIC_API_BASE_URL=https://api.mahasoftcorporation.com
NEXT_PUBLIC_WS_URL=wss://api.mahasoftcorporation.com
NEXT_PUBLIC_APP_SHELL_HOSTNAMES=mahasoftcorporation.com,www.mahasoftcorporation.com

NEXT_PUBLIC_API_TIMEOUT=30000
NEXT_PUBLIC_APP_NAME=Filling Station ERP
NEXT_PUBLIC_APP_VERSION=1.0.0
NEXT_PUBLIC_ENABLE_WEBSOCKET=false
NEXT_PUBLIC_ENABLE_NOTIFICATIONS=true
