{
  "name": "fserp-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor Android shell for FSERP (loads the live web app)",
  "scripts": {
    "sync": "cap sync android",
    "open:android": "cap open android",
    "build:android": "cap sync android && cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^7.6.0",
    "@capacitor/core": "^7.6.0",
    "@capacitor/splash-screen": "^7.0.5",
    "@capacitor/status-bar": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.6.0",
    "typescript": "^5.7.2"
  }
}
